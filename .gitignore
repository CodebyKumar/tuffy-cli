# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments (includes .venv/.tuffy_cuda_ready, the Jetson CUDA-build marker)
.venv

# Test cache
.pytest_cache/

# Model weights (large files)
src/models/weights/*/
src/voice/weights/whisper/
src/voice/weights/piper/

# User memory / personal data
data/

# Agent workspace (temporary execution artifacts)
agent_workspace/

# Connected MCP servers' own stderr output (startup banners, warnings) —
# see src/tools/mcp_client.py's MCP_SERVER_LOG_PATH
logs/

# OS-specific files
.DS_Store

# IDE / Project local configurations
.vscode/
.idea/
.gemini/
.agents/
.claude/

# Editor / Extension packages
*.vsix

# Local MCP server config (may contain commands/env referencing local paths or secrets)
.tuffy/mcp.json

# Local user settings (e.g. chosen default model), machine-specific
.tuffy/settings.json

# Scratchpad skill's generated note data, machine-specific
.tuffy/skills/scratchpad/notes.json

# API keys / secrets, read directly by src/llm/openai_compatible_provider.py
.env

uv.lock