# agent_workspace/

This directory is the local agent's workspace. When Tuffy executes file operations or generates artifacts (like writing stories, coding scripts, or analyzing local files), they are read from and written to this directory.

This folder is excluded from version control to prevent local execution artifacts from polluting the repository.
